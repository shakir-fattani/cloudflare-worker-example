import { Env } from "../types";

class BillingService {
  static async generateInvoice(env: Env) {
    // generate invoices
  }

  static async cancelSubscription(env: Env) {
    
  }

  static async processPayment(env: Env) {

  }
}

export default BillingService;
